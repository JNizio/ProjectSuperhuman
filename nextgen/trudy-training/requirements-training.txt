# Optional dependencies for `trudy-training train` only.
# Pin these in the actual training environment once a CUDA/PyTorch matrix is selected.
torch>=2.4
transformers>=4.45
peft>=0.13
accelerate>=1.0
# bitsandbytes is required only for 4-bit/8-bit QLoRA.
bitsandbytes>=0.44; platform_system != "Darwin"
